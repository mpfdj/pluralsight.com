3-way handshake
syn
syn-ack
ack

# OSI model
wires, radio or glass 			- physical
MAC address Ethernet protocol 	- data link
IP address IP protocol 			- network (routers)
TCP 3way handshake 				- transport
								- session (HPPS)
								- presentation
HTTP							- application

default gateway (router)

# TCP/IP model

# Encapsulation
5 - chunk                  - application layer
4 - segment                - transport layer
3 - packet                 - network layer (internet layer - TCP/IP
2 - frame                  - data link layer (local communication)
1 - 1/0 electrisch signaal - physical layer


# Data networks and encapsulation
router = gateway = default gateway (layer 3 device)
ethernet - frame - MAC address (hexadecimal) - hardcoded on the NIC - local communication
router - inside MAC address - outside MAC address

local addressing - MAC address
global addressing - IP Address, Subnet Mask, Default Gateway

